<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <link rel="stylesheet" href="/style.css">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Pacifico&family=Raleway:wght@300&display=swap" rel="stylesheet">
</head>
<body class="center view-100">
    <div>
        <form class="flex-column" method="post" <%if(user != null) { %>
                                action="/edit/<%=user._id%>"
                            <%}else{%>
                                action="/add"
                            <%}%>>

            <h1 class="w-100" id="register-glow">Register</h1>
            <div class="grid-split pad-1">
                <div class="flex-column m-1">
                    <label for="firstName">First Name</label>
                    <input type="text" id="firstName" name="firstName" placeholder="First Name" <%if(user != null) {%>
                    value=<%=user.firstName%>
                    <%}%>>
                </div>

                <div class="flex-column m-1">
                    <label for="lastName">last Name</label>
                    <input type="text" id="lastName" name="lastName" placeholder="Last Name" <%if(user != null) {%>
                        value=<%=user.lastName%>
                        <%}%>>
                </div>

                <div class="flex-column m-1">
                    <label for="organization">Organization</label>
                    <input type="text" id="organization" name="organization" placeholder="Organization" <%if(user != null) {%>
                        value=<%=user.organization%>
                        <%}%>>
                </div>

                <div class="flex-column m-1">
                    <label for="email">Email</label>
                    <input type="text" id="email" name="email" placeholder="Email" <%if(user != null) {%>
                        value=<%=user.email%>
                        <%}%>>
                </div>

                <div class="flex-column m-1">
                    <label for="phoneNumber">Phone Number</label>
                    <input type="text" id="phoneNumber" name="phoneNumber" placeholder="Phone Number" <%if(user != null) {%>
                        value=<%=user.phone%>
                        <%}%>>
                </div>

                <div class="flex-column m-1">
                    <label for="address">Address</label>
                    <input type="text" id="address" name="address" placeholder="Address" <%if(user != null) {%>
                        value=<%=user.address%>
                        <%}%>>
                </div>

                <div class="flex-column m-1">
                    <label for="city">City</label>
                    <input type="text" id="city" name="city" placeholder="City" <%if(user) {%>
                        value=<%=user.city%>
                        <%}%>>
                </div class="flex-column m-1">

                <div class="flex-column m-1">
                    <label for="country">Country</label>
                    <input type="text" id="country" name="country" placeholder="Country" <%if(user) {%>
                        value=<%=user.country%>
                        <%}%>>
                </div>
            </div>

            <button type="submit" class="m-1 w-25 font-medium">Submit</button>
        </form>
    </div>
</body>
</html>